# MOEX Wall Trader

Торговый робот для Мосбиржи (данные Finam Trade API): детект **стен** и **айсбергов**
в стакане, отслеживание их жизненного цикла (hit-счётчик, проедание) и торговля
от них — отбой на 1-м касании, пробой съеденной стены — с **AI-гейтом**
(Laya / Jev-совместимый) поверх сигналов.

> **DRY_RUN only.** Боевых ордеров нет — лимитки виртуальные, филлы симулируются
> по реальным принтам ленты (post-only честно: bid-заявку бьёт только sell-принт).
> Мейкер-мейкер: вход и выход лимитками, комиссия 0.

## Архитектура

```
collector_stream.py  СБОР ДАННЫХ (v2): gRPC-подписка SubscribeLatestTrades —
                     каждая сделка пушем (задержка p50 0.6с), без пропусков и
                     без расхода rate limit. Стакан (все уровни) раз в 2с.
                     Фолбэк на поллинг 0.35с при падении стрима. NDJSON-ротация по дням.
collector.py         Сбор v1 (поллинг) — оставлен как fallback.
walls_v2.py          SmartWallTracker: стены + айсберги + hit-счётчик +
                     разделение касаний. Сигналы:
                       rejection        — 1-е касание живой стены (отбой)
                       rejection_iceberg— касание айсберга (2+ удара, объём вернулся)
                       breakout         — стену проели (eaten, 3+ атаки) -> пробой
                       pull             — стену сняли без боёв (спуфинг)
                     Каждый сигнал несёт фичи: cvd, flow_imbalance, trend 5м,
                     day-range position, spread, wall_ratio/hits/age/eaten/iceberg.
walls.py             WallTracker v1 (базовый, без айсбергов).
wall_tui.py          Терминальный дашборд (rich): стакан с барами и стенами/айсбергами,
                     лента с агрессором и крупными принтами ⚡, свечи, кластеры
                     объёма за день (█buy ▓sell, POC), панель Laya-гейта с
                     вердиктами и живым P/L A/B (allow vs block, тейк/стоп ±5 тиков).
                     Режимы: live (gRPC-стримы) и --replay NDJSON с ускорением.
laya_gate.py         AI-гейт: HTTP на laya-serve (Jev-совместимый /v1/systemone).
                     Fallback на стеновой сигнал при падении сервера.
trader.py / laya_wall_trader.py   Live-циклы (DRY_RUN).
backtest_gate.py     Бэктест A/B: стратегия с гейтом и без на дневном NDJSON.
                     Гейт — детерминированная реплика (cvd/trend/imb, блок при 2+ против).
backtest_breakout.py Бэктест breakout-eaten стратегии + viz.
backtest_history.py / backtest_sim6.py  Бэктесты на архиве (arena_data.db).
test_hits_breakout.py  Тест hit-счётчика как фильтра пробоя.
viz_tests.py / viz_improvements.py  Отчёты-визуализации.
stats_events.py      Статистика прогона из events.jsonl.
test_walls.py        Юнит-тесты (8/8 OK).
```

Родственный проект: [finam-connector](https://github.com/antonBy77/finam-connector) —
единый пакет (клиент API + аналитика + viz), сюда можно перейти одним импортом.

## Запуск

```bash
export FINAM_TOKEN="твой токен Finam Trade API"   # или set -a && source .env && set +a

# полный сбор дня (стриминг): лента без пропусков + стакан 2с
python3 collector_stream.py --symbols SBER@MISX --hours 10

# бэктест A/B (без гейта / с гейтом-репликой)
python3 backtest_gate.py --strategy rejection    # или breakout

# терминальный дашборд LIVE: стакан, лента, свечи, кластеры, Laya P/L
FINAM_TOKEN=... LAYA_URL=http://127.0.0.1:8015 \
  python3 wall_tui.py --symbol SBER@MISX

# дашборд на исторических данных (токен не нужен, день за ~2.5 мин на x30)
LAYA_URL=http://127.0.0.1:8015 \
  python3 wall_tui.py --replay data/SBER-MISX-2026-09-24.ndjson --speed 30 --candle-sec 60

# тесты
FINAM_TOKEN=x python3 test_walls.py
```

Панель Laya-гейта в TUI: каждый сигнал (REJECTION/BREAKOUT) уходит в laya-serve
(`/v1/systemone`, type=choice, критерии allow/block/escalate). Вердикт с
P(allow) показывается сразу; сделка (виртуальная, тейк/стоп ±5 тиков, таймаут
10 мин) закрывается по ленте и попадает в счёт **ALLOW vs BLOCK** — строка
«гейт +X.XX₽ (allow-block)» = сколько гейт спас/потерял, блокируя входы.

## Результаты (SBER@MISX, 24.09.2026, полный день, DRY_RUN)

Валидация гипотезы стен на живом полном стакане (стриминг):

| Уровень | Устоял через 60с | n |
|---|---|---|
| **Стена (≥3× соседей)** | **82.7%** | 6 960 |
| Обычный уровень | 76.2% | 48 375 |

**Edge стен +6.5%, z=13.2** (на архиве было +5.6%, z=6.3 — эффект воспроизвёлся и усилился).
Дозировка подтверждена: 5×+ стены крепче обычных (z=9.5).

Бэктест rejection (тейк 5п, мейкер-мейкер, 1 день):

| Вариант | Сделок | PnL (1 лот) | Win |
|---|---|---|---|
| Без гейта | 221 | +9.37 ₽ | 89% |
| С гейтом cvd/trend/imb | 145 | +6.41 ₽ | 92% |

Breakout (съеденная стена, 3+ атак): 11 сделок, +0.59 ₽, win 73% (без гейта).

**Урок дня:** классический flow-гейт («поток против входа — блок») вреден для
отбоя: отбой по природе контртрендовое касание, поток против — это его суть.
Гейту для rejection нужен вопрос «хватит ли стене сил сломать поток?»
(wall_ratio, скорость проедания, близость к дневному экстремуму), а не
«за или против поток». Направленный flow-гейт оставить для breakout/pull.

Однодневная выборка, узкий канал — в тренде отбои будут хуже; тейк фиксированный.

## Требования

- Python 3.11+, токен Finam Trade API
- `pip install FinamPy grpcio protobuf matplotlib` (или venv)
- Для AI-гейта: [laya](https://huggingface.co/convaiinnovations/laya) (`pip install "laya[serve]"`)

## Часы работы

MOEX: будни 10:00–23:50 MSK, клиринги 14:00–14:05 и 18:45–19:00.
laya-serve держать поднятым только в торговые часы (cron 09:55/23:55 будни).
Вне сессии стакан пуст, стрим молчит.

## Дисклеймер

Экспериментальный исследовательский код. Не инвестиционная рекомендация.
Не подключён к боевым деньгам.
